package com.dispersion.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/dispersion")
public class SpillController {
    // This controller is now empty after moving the duplicate method.
    // You can add other spill-specific endpoints here if needed, or delete the
    // file.
}
